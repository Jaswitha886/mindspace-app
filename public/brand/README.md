# Brand assets

Replace these two files with the approved institutional artwork when it is
available:

- `mindspace-logo.svg`
- `srm-logo.svg`

Keep the filenames and SVG format so the application lockup does not need code
changes. The supplied files are neutral placeholders, not official SRM artwork.
